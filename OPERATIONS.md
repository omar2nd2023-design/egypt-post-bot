# تشغيل بوت تتبّع البريد المصري — 24/7 بدون الكمبيوتر الشخصي

> مافيش أي سر في الملف ده. كل القيم الحسّاسة بتتحط كـsecrets في
> Cloudflare وOrkestr، ومابتدخلش git أبدًا.

---

## 1) المعمارية

```
تليجرام ──▶ Cloudflare Worker ──▶ getToken() ──▶ KV
                    │                              ▲
                    │ لو التوكن خلص                 │ التوكن الجديد
                    ▼                              │
            خدمة التجديد (Orkestr)  ───────────────┘
                    │
                    ▼  متصفح حقيقي، دخول طبيعي
            digital.gov.eg  →  Keycloak  →  توكن
                    │
                    ▼
            apis.digital.gov.eg/actions  →  رحلة الشحنة
```

**ليه متصفح؟** التوكن عمره 900 ثانية، والـscope `fnf nid email username`
**مافيهوش `offline_access`** — يعني مفيش refresh token طويل المدى. وF5
Shape بيرفض أي طلب HTTP مش من متصفح حقيقي على `login.di.gov.eg`. فالتجديد
الوحيد اللي بيعدّي هو دخول طبيعي بمتصفح.

**ليه Orkestr؟** Cloudflare Workers ماتشغّلش متصفح. Orkestr بتشغّل حاوية
Docker فيها Chromium، من غير كارت ائتمان، ومن egress (OVH Strasbourg،
AS16276) مثبت إنه بيوصل للنطاقات الثلاثة.

**Chromium بيشتغل عند التجديد بس** — مش دائم التشغيل. الحاوية بتقعد
ساكنة، وبتفتح متصفح لما ييجي `POST /renew`، وبتقفله بعدها في `finally`.

---

## 2) منع تكرار الدخول — ثلاث طبقات

| الطبقة | فين | الضمان |
|---|---|---|
| وعد موحَّد (`inflightRenew`) | Worker | **محسوم** جوّه الـisolate الواحد |
| قفل KV (`renew_lock`, TTL 120ث) | Worker | **احتمالي** — KV اتساقها مؤجّل |
| single-flight داخل العملية | خدمة التجديد | **محسوم نهائيًا** — حاوية واحدة |

الطبقة الأولى اتضافت بعد ما الاختبار المتزامن أمسك باج حقيقي: `acquireLock`
بيعمل قراية-بعدين-كتابة، والـ`await` بينهم بيسمح لعشر طلبات تعدّي كلها.
الطبقة التالتة هي خط الدفاع الأخير: مهما حصل فوق، دخول واحد بس بيتنفّذ.

---

## 3) سلوك مدير التوكن

| الحالة | التصرّف |
|---|---|
| فاضل > 120ث | يرجّعه على طول |
| 30–120ث | يرجّعه **حالًا**، ويجدّد في الخلفية (`waitUntil`) — المستخدم مايستناش |
| < 30ث أو منتهي | يجدّد ويستنى (100ث كحد أقصى)، والرسالة بتتحدّث كل ~6ث |
| الـAPI رد 401 | تجديد إجباري + إعادة **واحدة بس**، ولازم التوكن يكون مختلف |
| التجديد فشل + القديم لسه صالح | يكمّل بالقديم |
| التجديد فشل + القديم منتهي | فشل محكوم، ورسالة واضحة للمستخدم |

**التوكن مابيتمسحش أبدًا.** مافيش `KV.delete(TOKEN_KEY)` في الكود كله.
و`POST /token` بيرفض يخزّن توكن منتهي فوق واحد سليم.

---

## 4) KV — أقل حاجة ممكنة

| المفتاح | المحتوى | العمر |
|---|---|---|
| `de_token` | التوكن (سرّي) | دائم، بيتكتب فوقه |
| `renew_lock` | ختم وقت | 120 ثانية، بينتهي لوحده |

عمر التوكن جوّه الـJWT نفسه، فمافيش metadata منفصلة تروح تتعارض معاه.

---

## 5) متغيّرات البيئة

### Cloudflare Worker

| المتغيّر | نوعه | الغرض |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | secret | البوت |
| `TURSO_URL` / `TURSO_TOKEN` | secret | فهرس الباركود |
| `ADMIN_SECRET` | secret | يحمي `POST /token` |
| `WEBHOOK_SECRET` | secret | يحمي `POST /webhook` |
| `RENEWER_URL` | var | رابط خدمة التجديد |
| `RENEW_SECRET` | secret | يحمي `POST /renew` |
| `GITHUB_TOKEN` / `GITHUB_REPO` | secret / var | الاحتياطي القديم (اختياري) |

### خدمة التجديد (Orkestr)

| المتغيّر | نوعه | الغرض |
|---|---|---|
| `DE_PHONE` | secret | موبايل الدخول |
| `DE_PASSWORD` | secret | كلمة السر |
| `WORKER_URL` | var | فين يرفع التوكن |
| `ADMIN_SECRET` | secret | **نفس قيمة الـWorker** |
| `RENEW_SECRET` | secret | **نفس قيمة الـWorker** |
| `PROBE_KEY` | secret | اختياري — يحمي `/f5` |

بيانات الدخول بتتقرا من البيئة بس، بتتستخدم في مكان واحد
(`_do_renew`)، ومابتظهرش في أي لوج ولا رد.

---

## 6) النشر

**Worker:** `npx wrangler deploy` من `cloud-bot/`.
الأسرار: `npx wrangler secret put <NAME>`.

**خدمة التجديد:** Orkestr → الخدمة → Redeploy.
Build context: `orkestr-f5-test` · Dockerfile: `orkestr-f5-test/Dockerfile`.

---

## 7) الرجوع للخلف (rollback)

| المشكلة | الإجراء |
|---|---|
| الـWorker باظ | `npx wrangler rollback` أو نشر الـcommit السابق |
| التجديد باظ | Orkestr → Deployments → Redeploy لنسخة أقدم |
| التوكن اتخزّن غلط | مافيش خطر: `/token` بيرفض المنتهي، والقديم بيفضل |

الرجوع آمن لأن التغييرات كلها في الكود — مافيش migration ولا schema.

---

## 8) أنماط الفشل

| العطل | الأثر | التعافي |
|---|---|---|
| Orkestr نايمة | أول تجديد أبطأ (cold start) | تلقائي |
| Orkestr واقعة | مفيش تجديد | القديم يشتغل لحد ما يخلص، وبعدها رسالة واضحة |
| بيانات الدخول اتغيّرت | كل تجديد يفشل | `/health` يوري `token: expired` — لازم تدخّل يدوي |
| F5 غيّر سلوكه | الدخول يفشل | `failure_reason` في رد `/renew` بيقول عند أي خطوة |
| KV اتأخّرت | تجديد زيادة نادر | single-flight في الخدمة بيمتصّه |
| الـWorker اترستر | `inflightRenew` بيروح | القفل في KV والخدمة لسه شغّالين |

---

## 9) المراقبة

`GET /health` على الـWorker — كلها أرقام وحالات، مافيهاش أي سر:

```json
{ "ok": true, "token": "valid", "token_exp_in_sec": 612,
  "needs_renew": false, "renew_lock_held": false,
  "renewer_configured": true }
```

`GET /health` على خدمة التجديد بيوري إعداداتها وآخر نتيجة تجديد.

**إنذار مقترح:** `token_exp_in_sec == 0` أو `null` لمدة > 5 دقايق
معناها التجديد واقف.

---

## 10) الأمان

- مفيش أي سر في git — بس مراجع لمتغيّرات بيئة.
- مفيش `console.*` في الـWorker إطلاقًا — صفر احتمال تسريب في اللوجات.
- التوكن مابيرجعش في أي رد HTTP، ولا في أي URL، ولا لتليجرام.
- خدمة التجديد بترجّع metadata بس (`login_success`، `api_status`،
  `elapsed_ms`، `failure_reason`) — مفيش توكن ولا بيانات دخول.
- `redact()` بيمسح JWT/Bearer/Authorization/password من أي نص جاي
  من الصفحة قبل ما يدخل أي مخرجات.
- **اتشالت 5 نقاط تشخيصية كانت مفتوحة للعامة** (`/probe`, `/probejs`,
  `/kc-probe`, `/reach`, `/debug`). `/debug` كان بينادي API مصر الرقمية
  بتوكننا ويرجّع الرد الخام لأي حد، و`/probejs?u=` كان SSRF كامل.
- الطلبات الخارجية الوحيدة الباقية: Telegram · Turso · مصر الرقمية ·
  خدمة التجديد. كلها لوجهات ثابتة في الكود.
